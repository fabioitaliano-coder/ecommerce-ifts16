# Copiar este archivo a ".env" para trabajar localmente.
# En clase usamos SQLite porque no requiere instalar otro motor.

DB_DIALECT=sqlite
DB_STORAGE=./database.sqlite
DB_LOGGING=false
JWT_SECRET=cambiar_esta_clave_por_una_mas_segura
