# E-Commerce de Hardware IFTS 16

API RESTful de ejemplo para la clase 6, reorganizada con estructura MVC y frontend estático en `public/`.

## Tecnologías

- Node.js
- Express
- JavaScript

## Instalación

```bash
npm install
npm start
```

El servidor queda disponible en `http://localhost:3080`.

## Estructura

```text
/public
/src
  /controllers
  /routes
  /models
  /data
server.js
README.md
```

## Endpoints

- `GET /api/productos`
- `GET /api/productos/:id`
- `POST /api/productos`
- `PUT /api/productos/:id`
- `DELETE /api/productos/:id`
- `GET /api/clientes`
- `GET /api/clientes/:id`
- `POST /api/clientes`
- `PUT /api/clientes/:id`
- `DELETE /api/clientes/:id`
- `GET /api/descuentos`
- `GET /api/descuentos/:id`
- `POST /api/descuentos`
- `PUT /api/descuentos/:id`
- `DELETE /api/descuentos/:id`
- `POST /api/checkout`

## Integrantes

- Fabio



